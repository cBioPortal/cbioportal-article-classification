# NCBI/PubMed Configuration (required)
NCBI_EMAIL=your.email@example.com

# Optional: NCBI API key for higher rate limits (10 req/sec vs 3 req/sec)
# Get one at: https://www.ncbi.nlm.nih.gov/account/settings/
NCBI_API_KEY=

# Unpaywall Configuration (optional, improves PDF download success rate)
# Can reuse the same email as NCBI_EMAIL if not specified
UNPAYWALL_EMAIL=your.email@example.com

# AWS Bedrock Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default
BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0

# Optional: S3 bucket for future PDF storage
S3_BUCKET_NAME=your-bucket-name
